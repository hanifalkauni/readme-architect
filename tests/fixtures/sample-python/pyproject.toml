[tool.poetry]
name = "sample-fastapi-service"
version = "1.0.0"
description = "FastAPI backend service with Celery and Redis"
authors = ["Test <test@example.com>"]
license = "Apache-2.0"

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.111.0"
uvicorn = "^0.30.0"
sqlalchemy = "^2.0.0"
